# Comment the next line if you don't want to use dynamic frameworks
use_frameworks!

def pods
  pod 'SVGView', :path => '../SVGView/'
  pod 'Macaw'
end

target 'SVGViewTests (iOS)' do
  platform :ios, '14.0'
  pods
end

target 'SVGViewTests (macOS)' do
  platform :osx, '11.0'
  pods
end
